# Execution Evidence

After running the Colab notebook, place the generated files here and commit them:

- `soc_events.jsonl`
- `execution_summary.json`
- screenshots showing LLM tool calls, blocked injection, retry, HITL, and persistence

Do not commit API keys or `.env`.
